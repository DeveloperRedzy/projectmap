import { styled } from '@mui/material';
import IconButton from '@mui/material/IconButton';

const WhiteButton = styled(IconButton)`
  height: 2em;
  width: 2em;
  background: white;
`;

const ProjectAccordionMVButton = styled(IconButton)`
  height: 2em;
  width: 2em;
  background: #fafafa;
`;

export { WhiteButton, ProjectAccordionMVButton };
